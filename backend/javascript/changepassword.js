"This script will send the user an email asking them to change their password"
send user email = 'Your password has request to be changed, to approve press CHANGE, to deny press DENY.'
if user pressed CHANGE = Reset password in database - User can now input new password in login screen

if user presses DENY = Abort process
